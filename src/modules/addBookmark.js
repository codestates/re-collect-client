import { initialState } from "./initialState";

const bookmarkReducerX = (state = initialState, action) => {
  switch (action.type) {
    case "ADD":
      return;
    case "EDIT":
      return;
  }
};
